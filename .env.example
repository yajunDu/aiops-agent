# 复制为 .env 并填写实际值
# 项目根目录（留空则自动推断）
AIOPS_ROOT=

# Neo4j 密码（部署 Neo4j 时设置的）
NEO4J_PWD=your-neo4j-password

# Qwen 模型路径（本地权重目录）
VLLM_MODEL_PATH=/path/to/Qwen2.5-7B-Instruct-AWQ

# 实时诊断模式（smart_chat 会自动设为 1，无需手动改）
AIOPS_LIVE_MODE=0
