apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-kill-seat
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces: [train-ticket]
    labelSelectors:
      app: ts-seat-service
  gracePeriod: 0
